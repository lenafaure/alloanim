<%= form_with(model: circonscription, local: true) do |form| %>
  <% if circonscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(circonscription.errors.count, "error") %> prohibited this circonscription from being saved:</h2>

      <ul>
      <% circonscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :circonscription_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
