<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'AlloAlloAnim', root_path ,class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
   <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
     <% if (user_signed_in? || center_signed_in?) %>
       <% if can? :read, User %>
        <ul class="nav navbar-nav">
          <li><%= link_to "Recherche d'animateurs", users_path, method: :get %></li>
        </ul>
       <% end %>
       <% if can? :read, Offer %>
           <ul class="nav navbar-nav">
             <li><%= link_to "Voir les offres", offers_path, method: :get %></li>
           </ul>
       <% end %>
       <!-- <form class="navbar-form navbar-left">
         <div class="form-group">
           <input type="text" class="form-control" placeholder="Search">
         </div>
         <button type="submit" class="btn btn-default">Submit</button>
       </form> -->
     <% end %>

      <ul class="nav navbar-nav navbar-right">
        <% if (!user_signed_in? && !center_signed_in?) %>
          <li class="menu_link"><%= link_to "S'inscrire", new_user_registration_path, method: :get %></li>
          <li class="menu_link"><%= link_to "Se connecter", user_session_path, method: :get %>
          <li class="menu_link btn btn-pink btn-slim"><%= link_to 'Accès recruteur', center_session_path, method: :get %></li>
        <% else %>
          <% if user_signed_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                <%= image_tag avatar_url(current_user), class: 'aaa-img-circle avatar-small' %>
                <% if current_user.first_name.present? %>
                    <%= current_user.first_name %>
                    <span class="caret"></span>
                <% end %>
              </a>
              <ul class="dropdown-menu">
                <li><a href="#"><%= link_to 'Mon profil', user_path(current_user) %></a></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to 'Déconnexion', destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
          <% else if center_signed_in? %>
            <li class="menu_link create_offer"><i class="fa fa-lg fa-plus-square"></i><%= link_to "Créer une offre", new_offer_path, method: :get %></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                <%= current_center.email %> (REV)
                <span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <li><a href="#"><%= link_to 'Mon Profil', center_path(current_center) %></a></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to 'Déconnexion', destroy_center_session_path, method: :delete %></li>
              </ul>
            </li>
           <% end %>
        <% end %>
      <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>