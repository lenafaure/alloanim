<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'AlloAlloAnim', root_path ,class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
   <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Animateurs', users_path, method: :get %></li>
      </ul>
     <!-- <form class="navbar-form navbar-left">
       <div class="form-group">
         <input type="text" class="form-control" placeholder="Search">
       </div>
       <button type="submit" class="btn btn-default">Submit</button>
     </form> -->

      <ul class="nav navbar-nav navbar-right">
        <% if (!user_signed_in?) %>
          <li class="menu_link"><%= link_to 'Inscription', new_user_registration_path, method: :get %></li>
          <li class="menu_link"><%= link_to 'Connexion', user_session_path, method: :get %></li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              <%= image_tag avatar_url(current_user), class: 'aaa-img-circle avatar-small' %>
              <% if current_user.first_name.present? %>
                  <%= current_user.first_name %>
                  <span class="caret"></span>
              <% end %>
            </a>
            <ul class="dropdown-menu">
              <li><a href="#"><%= link_to 'Mon profil', user_path(current_user) %></a></li>
              <li role="separator" class="divider"></li>
              <li class="menu_link"><%= link_to 'Déconnexion', destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
      <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>