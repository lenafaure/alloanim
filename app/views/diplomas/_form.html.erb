<%= form_with(model: diploma, local: true) do |form| %>
  <% if diploma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diploma.errors.count, "error") %> prohibited this diploma from being saved:</h2>

      <ul>
      <% diploma.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :diploma_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
