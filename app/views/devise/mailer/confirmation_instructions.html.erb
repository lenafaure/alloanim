<p><%= t('.greeting', recipient: @resource.email, default: "Bienvenue sur AlloAnim, #{@resource.first_name}!") %></p>

<p><%= t('.instruction', default: 'Confirmez votre compte AlloAnim en cliquant sur ce lien:') %></p>

<p><%= link_to t('.action', default: 'Confirmer mon compte'),
         confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale) %></p>
